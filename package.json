{
  "name": "hftech",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "cd backend && node server.js",
    "prebuild": "cd backend && npm run build",
    "build": "cd frontend && npm run build"
  }
}
